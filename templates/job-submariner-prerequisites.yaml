apiVersion: batch/v1
kind: Job
metadata:
  name: submariner-prerequisites-checker
  namespace: open-cluster-management
  labels:
    app.kubernetes.io/name: submariner-prerequisites
    app.kubernetes.io/component: health-check
  annotations:
    argocd.argoproj.io/sync-wave: "7"
spec:
  backoffLimit: 50
  activeDeadlineSeconds: 14400
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: prerequisites-checker
        image: {{ $.Values.ansible.containerImage }}
        env:
        - name: PRIMARY_CLUSTER
          value: {{ include "rdr.primaryClusterName" . | quote }}
        - name: SECONDARY_CLUSTER
          value: {{ include "rdr.secondaryClusterName" . | quote }}
        - name: SUBMARINER_BROKER_NAMESPACE
          value: {{ include "rdr.submarinerBrokerNamespace" . | quote }}
        command:
        - /bin/bash
        - -c
        - |
{{ include "rdr.ansibleStageOnly" . | indent 10 }}
            export PRIMARY_CLUSTER SECONDARY_CLUSTER SUBMARINER_BROKER_NAMESPACE
            SUB_SCRIPT="$STAGE/roles/submariner_prerequisites/files/submariner-prerequisites-check.sh"
            echo "Running Submariner prerequisites script with line-buffered output (kubectl logs -f will show progress)."
            if command -v stdbuf >/dev/null 2>&1; then
              exec stdbuf -oL -eL bash "$SUB_SCRIPT"
            else
              exec bash "$SUB_SCRIPT"
            fi
        volumeMounts:
        - name: ansible
          mountPath: /ansible-cm
          readOnly: true
      volumes:
      - name: ansible
        configMap:
          name: regionaldr-ansible
      serviceAccountName: submariner-prerequisites-checker
