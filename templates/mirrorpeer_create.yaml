{{- if eq "1" (include "rdr.odfPostInstallFixesEnabled" . | trim) }}
{{- range .Values.regionalDR }}
{{ $clusterSet := .name }}
{{ $clusters := .clusters }}
---
apiVersion: multicluster.odf.openshift.io/v1alpha1
kind: MirrorPeer
metadata:
  labels:
    cluster.open-cluster-management.io/backup: resource
  name: mirrorpeer-{{ $clusterSet }}
  annotations:
    argocd.argoproj.io/sync-wave: "8"
spec:
  items:
  - clusterName: {{ include "rdr.primaryClusterName" $ }}
    storageClusterRef:
      name: ocs-storagecluster
      namespace: openshift-storage
  - clusterName: {{ include "rdr.secondaryClusterName" $ }}
    storageClusterRef:
      name: ocs-storagecluster
      namespace: openshift-storage
  manageS3: true
  type: async
{{- end }}
{{- end }}
